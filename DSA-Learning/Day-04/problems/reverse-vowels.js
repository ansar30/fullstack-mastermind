/**
 * Problem: Reverse Vowels of a String
 * 
 * Given a string s, reverse only all the vowels in the string and return it.
 * Vowels: 'a', 'e', 'i', 'o', 'u' (both lowercase and uppercase)
 * 
 * Example:
 * Input: s = "hello"
 * Output: "holle"
 * 
 * Example 2:
 * Input: s = "leetcode"
 * Output: "leotcede"
 * 
 * Time Complexity Goal: O(n)
 * Space Complexity Goal: O(n) for result array
 */

function reverseVowels(s) {
    // Your solution here

}

// Test cases
console.log(reverseVowels("hello")); // Expected: "holle"
console.log(reverseVowels("leetcode")); // Expected: "leotcede"
console.log(reverseVowels("aA")); // Expected: "Aa"

module.exports = reverseVowels;
